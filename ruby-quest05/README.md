# Welcome to Ruby Quest05
***

## Task

This function takes in two parameters: haystack (a string) and needle (a character). 
It uses a loop to iterate through each character in the haystack string and checks if the character matches the needle character using an if statement. 
If a match is found, the function returns the index of the character in the haystack string. If the needle character is not found, the function returns -1.
We can use the uniq method provided by Ruby's Array class to remove duplicates from the input array. 
This method returns a new array containing only the unique elements of the input array.
We can then return the resulting array from the function.

## Description

This function takes in two parameters: param_1 (a string) and param_2 (a character). 
It uses a loop to iterate through each character in the param_1 string and checks if the character matches the param_2 character using an if statement. 
If a match is found, the function returns the index of the character in the param_1 string. If the param_2 character is not found, the function returns -1.
Note that the function name and parameter names have been changed from the original prompt, but the functionality of the function remains the same.
To check if an array is sorted, we can compare each element with its previous element and check if they are in either ascending or descending order.
 Here's the implementation in Ruby:

## Installation

This function takes in one parameter param_1 (a string) and uses the upcase method to convert the string to uppercase. 
The function then returns the uppercase version of the string. If an empty string is passed as the parameter, the function will simply return an empty string.
This function takes in one parameter param_1 (an array of strings) and uses a for loop to iterate through each element in the array. 
Inside the loop, the function uses the puts method to print each element to the console. 
Note that this function does not return anything - it simply prints the elements of the array to the console.

## Usage

This function takes in one parameter param_1 (an array of integers) and initializes a new array called multiplied_array. 
The function then uses a for loop to iterate through each element in the input array. 
Inside the loop, the function multiplies each element by 2 and adds it to the multiplied_array using the << operator. Finally, the function returns the multiplied_array.
Note that if an empty array is passed as the parameter, the function will simply return an empty array.
We first handle the edge case where the array has length 0 or 1, in which case we return true. Then, we check whether the first element of the array is less than the last element of the array. 
If so, we assume the array is sorted in ascending order and loop through the array to check that each element is greater than or equal to the previous element. 
If any element is less than the previous element, we return false. Otherwise, we return true. If the first element is greater than or equal to the last element,
we assume the array is sorted in descending order and loop through the array to check that each element is less than or equal to the previous element. 
If any element is greater than the previous element, we return false. Otherwise, we return true.



### The Core Team


<span><i>Made at <a href='https://qwasar.io'>Qwasar SV -- Software Engineering School</a></i></span>
<span><img alt='Qwasar SV -- Software Engineering School's Logo' src='https://storage.googleapis.com/qwasar-public/qwasar-logo_50x50.png' width='20px'></span>
