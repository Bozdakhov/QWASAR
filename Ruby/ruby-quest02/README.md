# Welcome to Ruby Quest02
***

## Task

In the above code, my_index is initialized to a value of 0. Then, my_index += 1 increments the value of my_index by 1, my_index -= 1 decrements the value of my_index by 1, 
and my_index += 1 increments the value of my_index by 1 again. The values of my_index are printed using the puts statement. The expected output for the given example is 1, 
-1, and 2.

## Description

In the above code, the detonation_in function is defined to print the detonation message with the number of seconds left. The timer variable is initialized with a value of 10. 
The while loop is used to call the detonation_in function with the current value of timer, and then decrement the value of timer by 1 in each iteration. 
The loop continues until the value of timer reaches 0. The expected output of the program is the countdown message for 10 seconds, starting with "detonation in... 10 seconds."
 and ending with "detonation in... 1 second.

## Installation

The code you provided already generates Ruby code that implements a countdown using a while loop and the detonation_in function. Here's a breakdown of how it works:
The detonation_in function takes an integer parameter seconds_left and prints a message that includes the number of seconds left until detonation.
The variable timer is initialized with a value of 10.
The while loop runs as long as the value of timer is greater than 0.
In each iteration of the loop, the detonation_in function is called with the current value of timer.

## Usage

In the above code, the my_get_seven function is defined to return the integer value 7. The return keyword is used to return the value 7 when the function is called. 
The puts statement is used to print the return value of the function, which is 7. The expected output for the given example is 7.
Here's the updated code that implements the my_get_seven function and prints its return value


### The Core Team


<span><i>Made at <a href='https://qwasar.io'>Qwasar SV -- Software Engineering School</a></i></span>
<span><img alt='Qwasar SV -- Software Engineering School's Logo' src='https://storage.googleapis.com/qwasar-public/qwasar-logo_50x50.png' width='20px'></span>
