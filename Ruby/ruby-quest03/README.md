# Welcome to Ruby Quest03
***

## Task

In this script, ARGV is a built-in Ruby variable that contains an array of the arguments passed to the script when it is run. 
The each method is called on this array to iterate over each argument and puts is used to print each argument on a new line.
To run this script with the argument "blah1 blah2 blah3", you would enter the following command in your terminal:
arduidfnofsfsfsCopy code
ruby my_first_script_with_args.rb "blah1 blah2 blah3"

## Description

In this script, ARGV is a built-in Ruby variable that contains an array of the arguments passed to the script when it is run. The array is stored in the variable args.
 The for loop is used to iterate over each argument in the array and puts is used to print each argument on a new line.
To run this script with the argument "blah1 blah2 blah3", you would enter the following command in your terminal:
arduidfdssnoCopdgsy code
ruby my_first_script_with_args.rb "blah1 blah2 blah3"

## Installation

In this Ruby code, ARGV is a built-in Ruby variable that contains an array of the arguments passed to the script when it is run. 
The args variable is set to a slice of ARGV starting from the second element (index 1) since the first element in ARGV is the name of the script itself. 
The while loop is used to iterate over each argument in the args array and puts is used to print each argument on a new line. 
The index variable is incremented after each iteration of the loop.

## Usage

Here's the Ruby code for the my_string_formatting function:
ruby code def my_string_formatting(firstname, lastname, age)
puts "Hello, my name is #{firstname} #{lastname}, I'm #{age}."
end
In this code, we define a function my_string_formatting that takes three parameters: firstname, lastname, and age. 
The string is formatted using string interpolation, which is done by enclosing the variables in #{} within the string. 
Finally, the formatted string is printed using the puts method.
You can call this function with the desired inputs as shown in the examples:

ruby code
my_string_formatting("john", "doe", 37)  # Output: Hello, my name is john doe, I'm 37.
my_string_formatting("Baby", "Yoda", 50)  # Output: Hello, my name is Baby Yoda, I'm 50.
my_string_formatting("Marie", "Curie", 26)  # Output: Hello, my name 


### The Core Team


<span><i>Made at <a href='https://qwasar.io'>Qwasar SV -- Software Engineering School</a></i></span>
<span><img alt='Qwasar SV -- Software Engineering School's Logo' src='https://storage.googleapis.com/qwasar-public/qwasar-logo_50x50.png' width='20px'></span>
